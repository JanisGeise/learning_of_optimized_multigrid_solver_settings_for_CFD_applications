#!/bin/sh
cd "${0%/*}" || exit                                # Run from this directory
. ${ML_CFD_BASE}/RunFunctions        # run functions
#------------------------------------------------------------------------------

# reset simulation
cleanCase

# delete o directory
rm -r 0

# remove computed cell centers
rm constant/C*
rm constant/V
#------------------------------------------------------------------------------
